// 8.7 Задание 5

let arr = ['Каждый', 'охотник', 'желает', 'знать', 'где', 'сидит', 'фазан'];
console.log(`В массиве ${arr.length} элементов`);
arr.forEach(element => {
  console.log(element);
});